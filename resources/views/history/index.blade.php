@extends('layouts.app')
@section('title', 'Riwayat Perhitungan')
@section('page-title', 'Riwayat Perhitungan')
@section('page-subtitle', 'Snapshot setiap proses TOPSIS yang pernah disimpan')
@section('content')
<div class="card"><div class="card-header d-flex justify-content-between align-items-center"><div><h5 class="mb-1 fw-bold">Daftar Perhitungan</h5><div class="small text-secondary">Riwayat tidak berubah ketika data master diedit.</div></div><a href="{{ route('process.index') }}" class="btn btn-primary btn-sm"><i class="bi bi-plus-circle me-1"></i> Hitung Baru</a></div><div class="card-body p-0">@if($runs->isEmpty())<div class="empty-state"><i class="bi bi-clock-history"></i><h5 class="mt-3">Belum ada riwayat</h5><a href="{{ route('process.index') }}" class="btn btn-primary mt-2">Mulai Perhitungan</a></div>@else<div class="table-responsive"><table class="table table-hover"><thead><tr><th>No.</th><th>Nama Perhitungan</th><th>Waktu</th><th>Jumlah Data</th><th>Alternatif Terbaik</th><th>Preferensi</th><th class="text-end">Aksi</th></tr></thead><tbody>@foreach($runs as $run)<tr><td><span class="code-badge">{{ $runs->firstItem() + $loop->index }}</span></td><td><strong>{{ $run->name }}</strong><div class="small text-secondary">{{ $run->code }}</div></td><td>{{ $run->created_at->format('d/m/Y') }}<div class="small text-secondary">{{ $run->created_at->format('H:i:s') }}</div></td><td><span class="badge text-bg-light">{{ $run->alternative_count }} alternatif</span> <span class="badge text-bg-light">{{ $run->criterion_count }} kriteria</span></td><td><strong>{{ $run->best_alternative_name }}</strong><div class="small text-secondary">{{ $run->best_alternative_code }}</div></td><td><strong>{{ decimal_value($run->best_preference) }}</strong></td><td class="text-end"><a href="{{ route('results.show', $run) }}" class="btn btn-sm btn-outline-primary"><i class="bi bi-eye"></i></a><form method="POST" action="{{ route('history.destroy', $run) }}" class="d-inline" data-confirm="Hapus riwayat {{ $run->code }} secara permanen?">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@endforeach</tbody></table></div>@endif</div>@if($runs->hasPages())<div class="card-footer bg-white">{{ $runs->links() }}</div>@endif</div>
@endsection
